#файл points.py
# 1. Lambda в методе min с ключом сортировки
special_point = min(points, key=lambda p: p[0] + p[1])  
# 🧠 Лямбда-функция принимает точку p, возвращает сумму её координат
# 🔧 Используется как ключ для поиска точки с минимальной суммой координат

special_point = min(points, key=lambda p: (p[0], p[1]))  
# 🧠 Лямбда-функция принимает точку p, возвращает кортеж (x, y)
# 🔧 Используется как ключ для лексикографической сортировки (сначала по x, потом по y)

# 2. List comprehension как аналог map
return [add_two_points(p, special_point) for p in points]
# 🧠 Применяет функцию add_two_points к каждой точке p в списке points
# 🔧 Аналог: map(lambda p: add_two_points(p, special_point), points)
# ⚡ Создает новый список с преобразованными точками

# 3. List comprehension с условием как аналог filter
other_points = [p for p in points if p != target]
# 🧠 Фильтрует список points, оставляя только точки, не равные target
# 🔧 Аналог: filter(lambda p: p != target, points)
# ⚡ Используется для исключения целевой точки из поиска ближайшей


#Файл distance.py
# 4. Lambda в функции min с пользовательским ключом
closest = min(other_points, key=lambda p: calc_dist(target, p))
# 🧠 Лямбда-функция принимает точку p, вычисляет расстояние до target
# 🔧 Используется как ключ для поиска точки с минимальным расстоянием
# ⚡ Более читаемая альтернатива: key=partial(calc_dist, target)


#Файл menu_state.py
# 5. Цикл for с применением функции (аналог map в императивном стиле)
for method_key, (method_code, method_name) in self.context.methods_map.items():
    result = process_points(self.context.points, method_code)
    print(f"{method_name}:")
    print(f"   Результат: {result}")
# 🧠 Применяет функцию process_points для каждого метода из словаря
# 🔧 Функциональный аналог: 
#   list(map(lambda item: process_points(points, item[1][0]), methods_map.items()))


#Файл input_data.py
# 6. List comprehension для генерации случайных точек
points = []
for i in range(n):
    x = random.randint(-10, 10)
    y = random.randint(-10, 10)
    points.append((x, y))
# ⚡ Функциональная альтернатива:
#   points = [(random.randint(-10, 10), random.randint(-10, 10)) for _ in range(n)]


#Файл view_state.py
# 7. Метод get словаря с лямбда-подобным значением по умолчанию
method_names.get(self.context.method, self.context.method)
# 🧠 Хотя это не лямбда, но работает похожим образом:
#   Если ключ есть - возвращает значение, если нет - возвращает сам ключ
# 🔧 Функциональный подход к безопасному получению значений из словаря
